
    const preloader = document.querySelector('.preloader');

  
    // 👇️ removes element from DOM
    menuToggle.addEventListener('setTimeout(()=> {}, 1000)', function() {
            preloader.classList.toggle('hilang');
    }


    // 👇️ hides element (still takes up space on page)
    // box.style.visibility = 'hidden';  // 👈️ time in millisecond